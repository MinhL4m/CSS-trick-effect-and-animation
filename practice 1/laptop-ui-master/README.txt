URL: https://www.youtube.com/watch?v=ZeDP-rzOnAA

HTML: 
- header tag has div for logo, nav for ul link, div for cart
- main tag has section. section divided into 2 parts. 1 div for introduction (text and buttons, each is separated div) and 1 div for cover(img)

CSS:
- TRICK: when edit, add background color for each div to find out space
- use flex for flex item. flex: 1
- use ul like a div that have li inside
- /*TRICK: to make the background color into the text color */
	-webkit-background-clip: text;
	-webkit-text-fill-color: transparent;
- make shadow for png. (filter)
-TRICK: to make background out of img, use position absolute and z-index
- animation at the start of the page.